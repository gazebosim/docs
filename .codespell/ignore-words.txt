# codespell ignore-words list
#
# Words listed here are treated as correctly spelled and will NOT be
# reported by codespell, even though they look like typos to the default
# dictionary. Use this for proper nouns, product names, and domain terms.
#
# Rules:
#   - one word per line, lowercase (matching is case-insensitive)
#   - keep the list alphabetically sorted
#   - lines starting with '#' are comments
#
# Add an entry here only when codespell flags a *correct* word
# (a false positive). For genuine typos, fix the source text instead.
infp
pincher
